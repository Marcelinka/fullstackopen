{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "cd ../phonebook && npm ci && npm run build && cp -r dist ../phonebook-backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}
